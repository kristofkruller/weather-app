{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "chrome",
          "request": "launch",
          "name": "Launch Chrome and start dev server",
          "url": "http://localhost:3050",
          "webRoot": "${workspaceFolder}",
          "preLaunchTask": "Run dev server"
      }
  ]
}
